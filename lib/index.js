const array1 = [1, 3, 5, 7, 2];

const newArray = array1.filter(function(item) {
  return newArray(item) >= 5;

});

// const filteredArr = array1.filter(n => n > 5) ;
// return filteredArr;

module.exports = array1;

newArray.map(item => `Wow ${item} is big!!`);
console.log(newArray.map);
