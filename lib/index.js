const array1 = [1, 3, 5, 7, 2];

const newArray = array1.filter(function(n) {
  return n >= 5;

});
return newArray;

// const filteredArr = array1.filter(n => n > 5) ;
// return filteredArr;

array1.map(`Wow ${n} is big!`);

module.exports = array1;
